import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.motovalor.app',
  appName: 'motovalor',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
