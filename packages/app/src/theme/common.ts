import { Theme } from 'react-native-elements'

const common: Theme = {
  Button: {
    buttonStyle: {
      borderRadius: 20
    }
  },
  SearchBar: {
    lightTheme: true,
    inputContainerStyle: {
      borderRadius: 10
    }
  },
  Icon: {
    size: 24
  }
}

export default common
