import { Theme } from 'react-native-elements'

const common: Theme = {
  Button: {
    buttonStyle: {
      borderRadius: 20
    }
  },
  SearchBar: {
    lightTheme: true,
    inputContainerStyle: {
      borderRadius: 10
    }
  }
}

export default common
