import React from 'react'
import { View, Text } from 'react-native'

const AccountScreen = () => {
  return (
    <View style={ { flex: 1, alignItems: 'center', justifyContent: 'center' } }>
      <Text>Hi from account screen</Text>
    </View>
  )
}

export default AccountScreen
